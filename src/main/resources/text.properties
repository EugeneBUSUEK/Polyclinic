query.select.user.by.login = SELECT USERS.ID, USERS.USERNAME, USERS.PASSWORD, USERS.NAME, USERS.BLOCKED, ROLES.NAME FROM USERS JOIN USERS_ROLES ON USERS.ID = USERS_ROLES.USER_ID JOIN ROLES ON USERS_ROLES.ROLE_ID = ROLES.ID WHERE USERS.USERNAME = ?